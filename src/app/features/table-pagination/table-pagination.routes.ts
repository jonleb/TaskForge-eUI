import { Routes } from '@angular/router';
import { TablePaginationComponent } from './table-pagination.component';

export const TABLE_PAGINATION_ROUTES: Routes = [
    { path: '', component: TablePaginationComponent },
];
